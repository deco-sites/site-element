import Component from "$store/components/ui/DivDrawer.tsx";
import type { Props } from "$store/components/ui/DivDrawer.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
